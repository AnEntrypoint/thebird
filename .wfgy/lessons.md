# Lessons

## Decision gates

- **react-lite.js vs preact adoption (2026-07-15)**: Consumers found: 1 — `Counter`/`reactCounterApp` in `docs/apps.js:1162-1193`, a proof-of-concept counter app (registered as the `counter` app, `docs/apps.js:1235`). Grep for `from './react-lite.js'` / `createRoot` / `useState` across `docs/*.js` surfaces only `docs/apps.js` importing it (`docs/apps.js:24`) and this single example component using it. Examined against react-lite.js's own header contract (useState-only, full-rerender, non-keyed reconciliation — see `docs/react-lite.js:19-30,89-94`): the Counter component uses one `useState` call and two buttons; no `useEffect`-shaped lifecycle side effect, no deep prop-drilling needing Context, no changing list needing keyed reconciliation. Genuine need count: 0 of 1 consumers (gate requires >=2). Decision: KEEP react-lite.js, do not adopt preact this pass. Since thebird's vendor-refresh pipeline is required for any real npm dependency add and is unavailable to an unsupervised background task, even a hypothetical "adopt" verdict could only be RECORDED here for a future pass with real vendoring capability, never implemented in this pass — moot given the 0-of-1 count. Re-check this gate if/when a second app consumes react-lite.js with a real effects/context/keyed-list friction point (not merely a hypothetical one).

- **instance-fs.js vs ZenFS/LightningFS swap (2026-07-15)**: PRD item evaluated: replace `docs/instance-fs.js`'s bespoke IndexedDB snapshot/persist machinery (in-memory object + debounced xstate-driven persist, `docs/instance-fs.js` full file, ~180 lines) with a maintained node-fs-shaped library over IndexedDB. Finding 1 (sync-read constraint is real): `docs/agent-run-verify.js:65` calls `instance.fs.readFile(p)` synchronously inside a `for` loop (`verifyWrittenFiles`, lines 61-70) and uses the return value immediately in the same statement (line 66) — no `await`. `instance-fs.js`'s `readFile`/`writeFile`/`exists`/`list` (lines 141-169) read/write an in-memory `snapshot` object directly; IDB persistence is a separate debounced side effect (`schedulePersist`, lines 89-91) that never gates the read/write path. Any replacement must preserve this synchronous shape or this call site breaks. Finding 2 (library shapes, from public knowledge only — not tested, no npm install in this session): ZenFS ships both sync and async backend variants, with sync-capable backends exposing a `fs.readFileSync`-style API mirroring Node's `fs`, matching the constraint. LightningFS is fully async/Promise-based by design per its own README — no sync API exists — disqualified outright as-is. Decision: PROTOTYPE-BEHIND-createFs-LATER, NOT SWAP-NOW, NOT REJECTED OUTRIGHT. Reasoning: (1) the sync constraint narrows the field to ZenFS-with-sync-backend only, LightningFS is out; (2) no npm-install capability exists in this unsupervised session to actually pull ZenFS and verify its sync-backend-over-IndexedDB path truly behaves synchronously end-to-end in a browser tab — sync-over-inherently-async-storage is the highest-risk unverified claim here; (3) project's own bar (a replacement library is adopted only when it nets a smaller maintained surface) isn't clearly cleared by swapping a full fs library in for ~180 lines without live proof; (4) correct next step if ever prioritized is a prototype *behind* the existing `createFs(instanceId, opts)` factory signature, proven live against `agent-run-verify.js`'s exact call pattern, before any swap lands. Finding 3 (factory seam is clean, with one bypass): every caller across `docs/` goes through `createFs(instanceId, opts)` (`docs/instance-fs.js:71`) and its returned `fs` object — no caller reaches into instance-fs's own `thebird-fs-<instanceId>` IDB store directly, so `createFs`'s internals are a valid future swap seam. Known issue found in passing, not fixed this pass, its own PRD-worthy row: `docs/terminal.js:6-33` implements an entirely separate legacy filesystem persistence path (`indexedDB.open('thebird', 1)`, store `'fs'`, key `'thebird_fs_v4'`, `idbLoad`/`idbSave`) that bypasses `createFs`/`instance-fs.js` completely — live code used in `terminal.js`'s own boot machine, not dead code, and distinct from unrelated separate DBs (`thebird-gui-state` in `os-shell.js:394`, `plugkit-wasm` in `freddie-host.js:1881`) which are different subsystems by design. Any future instance-fs work, including a ZenFS prototype, should fold `terminal.js` into scope or explicitly document its exemption from the single-fs-seam contract.

- **isomorphic-git already adopted; bespoke layer is a thin, working wrapper (2026-07-16)**: Measured LOC: docs/shell-git.js 527, docs/shell-node-git.js 62, docs/shell-git-auth.js 217 -- 806 total. Critical finding: docs/shell-node-git.js:1-11,30-62 already IS isomorphic-git -- it lazy-imports ./vendor/esm/isomorphic-git.mjs + ./vendor/esm/isomorphic-git-http-web.mjs and every makeGit() method (clone/init/add/commit/push/pull/fetch/status/statusMatrix/log/listBranches/branch/deleteBranch/hashBlob/checkout/resolveRef/currentBranch/remove/listRemotes/addRemote/deleteRemote/merge/readBlob/readTree/readCommit/tag/listTags) is a 1-line wrap(method) passthrough to the real isomorphic-git call -- this is not a swap candidate, isomorphic-git is the actual git engine running today. docs/shell-git.js (527 lines) is the CLI-command layer on top (arg parsing, porcelain-status formatting, unified-diff rendering for git diff/git commit stat lines, CORS-fallback to GitHub's Git Data API when smart-HTTP is blocked) -- UX glue isomorphic-git doesn't provide, not protocol reimplementation. docs/shell-git-auth.js (217 lines) is GitHub OAuth device-flow + the batched-blob CORS-fallback fetcher (fetchRepoBlobsBatched, atomicPushViaGitDataApi) -- a degraded-mode path for when isomorphic-git's smart-HTTP transport is CORS-blocked (shell-git.js:204-229 isCorsBlockedError catch). Working-functionality check: clone/push/pull/commit/checkout/branch/log/diff all call through to real isomorphic-git ops against a real IndexedDB-backed fs adapter (makeIdbFs, shell-git-auth.js:183-217); live exercised code, not a stub. Decision: NO SWAP NEEDED -- the PRD's premise ("bespoke git protocol reimplementation" swappable for isomorphic-git) is factually wrong; isomorphic-git is already the vendored engine. The only real "bespoke" surface is the CLI/UX/CORS-fallback layer (806 lines), which isomorphic-git cannot replace since it ships no CLI, diff-formatter, or GitHub-REST fallback. Close this PRD row as a documentation-mismatch, not an adoption gate.

- **BusyBox-WASM for awk/sed: gate not yet cleared, but plausibly clearable (2026-07-16)**: Measured LOC: docs/shell-awk.js 149, docs/shell-sed.js 220 -- 369 total, both pure-JS hand-rolled interpreters (awk: BEGIN/END/pattern-action blocks, field splitting/assignment, sub/gsub/split/sprintf; sed: full command set s p P h H g G x n N D b t a i c q + BRE/ERE translation + POSIX bracket classes + stateful address ranges). Other text-processing builtins found via grep over docs/shell-builtins-text.js (228 lines total): grep (line 23), sed (line 76, thin dispatcher into runSed from shell-sed.js -- not a duplicate), sort (line 111), uniq (line 149), tr (line 168), plus non-text which/exit. That's 4 real candidate builtins beyond awk/sed itself (grep, sort, uniq, tr), all small hand-rolled implementations in the same file. Gate requires "retiring >=3 other text builtins onto [BusyBox-WASM] simultaneously" -- with grep+sort+uniq+tr as candidates the count is plausibly >=3 IF BusyBox-WASM's coreutils applet set covers all four with byte-compatible-enough semantics (unverified -- no vendoring capability this session, no live BusyBox-WASM build inspected). Decision: GATE-NOT-YET-CLEARED this pass (nothing vendored or measured against a real BusyBox-WASM binary), but not a keep-bespoke-forever verdict either -- record for a future pass with real vendoring: if BusyBox-WASM is pulled in and confirmed to correctly replace grep+sort+uniq+tr (or more), retiring awk/sed onto the same provider clears the >=3 bar. Until then, keep all five as bespoke JS builtins.

- **multiplayer/game-sharing-between-users: out-of-scope-by-architecture, not a small addition (2026-07-16)**: PRD row + corner-case sibling "multiplayer-conflict-with-per-instance-isolation" evaluated against the real isolation boundary, confirmed live in docs/sw-instance.js: one Service Worker per instance at scope `./sw-i<N>/` (AGENTS.md "thebird per-instance SW isolation" memo), each owning its own per-id IndexedDB registry (`ownerClientId`/`ownerClientIds` at docs/sw-instance.js:86-87, populated only via the `claim-client` op at line 169-178 and gated by BROWSER-attested `sourceId` at lines 256-274 — an explicit anti-cross-tab-hijack boundary, not an oversight), with only the single key `thebird-last-instance` permitted in shared localStorage (AGENTS.md, corroborated — no other cross-instance channel exists in the SW/localStorage layer). (a) Isolation boundary confirmed real: this is a same-BROWSER, same-machine isolation mechanism (separating instances/tabs from each other's fs), not a network-facing mechanism at all — it has zero bearing on cross-user/cross-device state and could not be repurposed for multiplayer even if the isolation were loosened; a multiplayer channel is a categorically NEW subsystem, not a relaxation of an existing one. (b) What such a channel would minimally require: thebird is fully static/client-side (`bunx serve docs`, GH Pages, no server component of its own per AGENTS.md's layered-stack rule — thebird never fetches LLM providers or runs backend logic directly). BroadcastChannel/localStorage/SharedWorker are same-browser-only, cannot cross users or devices. WebRTC data channels are peer-to-peer post-connect but still need a signaling server for offer/answer/ICE exchange — thebird has none. The only existing network-facing surface in the stack is acptoapi/freddie (an LLM-chat gateway, per the "dynamic stack contract" memo: thebird -> freddie -> acptoapi, LLM-facing only) — repurposing it as a realtime-relay/rendezvous service for arbitrary game state would be building a new capability (message relay, room/session concept, presence, ordering/conflict resolution) onto infrastructure designed for one-shot chat completions, not a small addition. (c) Decision: OUT-OF-SCOPE-BY-ARCHITECTURE for this pass and the near-term backlog. Reasoning: no existing thebird-owned or freddie/acptoapi-owned component provides cross-user message transport today; every viable path (hosted relay via acptoapi, or a dedicated signaling server) is a new backend service outside thebird's zero-server, static-hosting contract, not a GUI/app-layer feature reachable by the level-editor/game-player closure this session built. If ever prioritized in a FUTURE pass with a real backend commitment, the minimal viable approach would be: (1) stand up a small WebSocket relay (could live alongside acptoapi's own daemon process, since that's the only piece of the stack that already runs a persistent server process) purely as a dumb room-based message forwarder — no game-state authority, no persistence — and (2) a thin thebird-side client in a NEW app (not level-editor/game-player, which stay single-player-authoring) that opens a channel keyed by a shared room code and layers optimistic client-side state sync (e.g. last-write-wins on entity positions) on top of the existing ECS tick from docs/lib/ecs.js. That is a multi-week new-subsystem effort, not a corner-case fix, and is correctly left off this session's closure scope.

- **no-code-game-authoring-flow-e2e: chain verified real, save/load paths match, no fix needed (2026-07-16)**: Traced the full non-coder flow built across this session's passes: level-editor (docs/level-editor-app.js) place objects via toolbar (+block/+actor/+goal) -> drag-reposition -> live inspector edit -> optional starter template from docs/templates/scenes/ (SCENE_TEMPLATES_BASE, level-editor-app.js:12) -> playtest via the extracted runScenePlaytest() runner (shared export, also consumed by game-player) -> save. Verified the save/load handoff is a REAL match, not two independently-shaped "scenes" concepts: level-editor-app.js:118 `const SCENE_DIR = 'level-editor'`, :302 `const SCENE_PATH = SCENE_DIR + '/scene.json'` (i.e. writes to fs key `level-editor/scene.json`), :392 `fs.writeJson(SCENE_PATH, scene)`. game-player-app.js:27-31 `listSceneCandidates(fs)` calls `fs.list('')` (empty prefix) then filters `k.endsWith('.json')` — deliberately generic rather than hardcoded to the level-editor path (per its own comment at game-player-app.js:22-26, to also surface export/save-as/hand-authored scenes elsewhere). Confirmed against the actual fs.list() semantics in docs/instance-fs.js:166-169: `list(prefix='')` does `Object.keys(snapshot).filter(k => k.startsWith(toKey(prefix)))` — an empty prefix matches every key, so `level-editor/scene.json` is unconditionally included in game-player's candidate list; the picker's `select` dropdown (game-player-app.js:48-53) then loads whichever key the user picks via `fs.readJson(path, null)` (game-player-app.js:122), and both level-editor and game-player import the same `runScenePlaytest`/`normalizeScene` functions from level-editor-app.js (game-player-app.js:13) so playback semantics are identical to the in-editor playtest, not a reimplementation. Result: MATCH CONFIRMED, no mismatch found, no fix required — the end-to-end chain (author -> save -> discover -> load -> play) is genuinely closed. The PRD row's original "CANNOT build any game via GUI alone" premise is now false and the row should close.

- **a10-editor-model-per-file-decision: SKIPPED, no editor library adopted in this pass, textarea remains the editing surface, so the model-per-file pattern has no library to attach to (2026-07-16)**: Checked .wfgy/lessons.md in full (all prior decision-gate entries above this one, covering react-lite/preact, instance-fs/ZenFS, isomorphic-git, BusyBox-WASM, multiplayer, no-code-authoring, xterm-readline) — zero B3/B9/editor-library-adoption entries exist from this integration effort; this gate's precondition ("only if Part B editor library decision actually adopts an editor lib") was never satisfied. Grepped `docs/*.js` for `monaco|codemirror` (case-insensitive): zero matches anywhere in docs/. The one non-docs hit, `docs/vendor/components/editor-primitives.js`, is a generic upstream design-kit primitives file (visual-only component library from anentrypoint-design per the GUI-boundary rule), not a Monaco/CodeMirror wrapper — grepped its own contents for the same terms with zero matches, confirming it is unrelated. thebird's actual code-editing surface today is `docs/level-editor-app.js` (the level editor app), which edits scene-object fields (`name`/`x`/`y`/`width`/`height`/`color`/`label`) via a live property-inspector form of plain `<input>` elements, not a textarea or any embedded code editor — there is no free-text code-editing surface in thebird at all right now, textarea or otherwise. Decision: SKIPPED. Reasoning: the row's own gate condition is an "only if" conditional on a Part B editor-library adoption that never happened — thebird has stayed dependency-lean per the project's zero-CSS/npm-@latest-only/vendor-refresh-gated constraints (AGENTS.md GUI boundary rule), and no B3/B9 pass in this effort pulled in Monaco or CodeMirror-6. With no adopted editor library, there is no `model` object (Monaco's `ITextModel` / CodeMirror's `EditorState`) to key a `Map<path, model>` against, so the model-per-file pattern has nothing to attach to. Re-check this gate only if/when a future pass genuinely adopts Monaco or CodeMirror-6 for a real multi-file code-editing surface.

- **c9-token-query-param-auth: SKIPPED, no proxy Bearer-clobber scenario exists to insert a query-param fallback into (2026-07-16)**: Grepped `docs/lib/acptoapi-browser.js` for `Authorization|Bearer|baseUrl`: the only two places this file ever sets an auth header are inside `callDirectProvider` (lines 318-323 — `headers['authorization'] = 'Bearer ' + key` for `auth:'bearer'` providers, `headers['x-api-key']`/`anthropic-version`/`anthropic-dangerous-direct-browser-access` for `auth:'anthropic'`), gated by the `DIRECT_PROVIDERS` map (lines 258-267: openai/groq/cerebras/mistral/openrouter/xai/deepseek/anthropic) — every one of those `fetch` calls (line 365, `timedFetch(cfg.url, ...)`) goes straight to the real provider's own public API endpoint (e.g. `https://api.groq.com/...`), never through any thebird- or acptoapi-owned proxy. The external-gateway path (`getBaseUrl()`, lines 128-141, hitting `:4800` or a configured `acptoapi.baseUrl`) sends only `{ 'content-type': 'application/json' }` as headers at both of its fetch call sites (lines 527, 550) — no Authorization/Bearer header is ever attached to a request routed through the acptoapi gateway/proxy. Cross-checked `docs/chat-config.js` for the same terms: only `baseUrl` hits (UI field for the external gateway URL, cross-origin-rejection guard at lines 224-242), zero `Authorization`/`Bearer` references — chat-config.js only ever configures the URL, never attaches credentials to a proxied request. Conclusion: the row's premise (Bearer header clobbering cached proxy credentials) does not correspond to any real code path — thebird never sends an Authorization header to a proxy/gateway it also expects to authenticate via cached/Basic credentials; direct-provider calls go over open internet straight to the provider (no proxy involved at all), and gateway calls carry no auth header to clobber in the first place. Decision: SKIPPED — no safe insertion point exists because there is no clobber scenario to guard against; a query-param auth fallback would be solving a problem that does not exist in this codebase. Re-check only if a future pass introduces a thebird-owned or acptoapi-fronting proxy that both accepts Basic auth AND is called with an Authorization header from the same client.

- **xterm-readline: keep bespoke, real gap in heredoc/multi-line/bang-history handling (2026-07-16)**: docs/shell-readline.js measured 185 lines. Catalog of what it actually implements: (1) history navigation (up/down via [A/[B, histNav, getHistory() reads window.__debug.shell.history); (2) cursor motion (left/right/home/end/Ctrl-A/Ctrl-E, [H/[F/[1~/[4~); (3) kill-to-end/kill-to-start via Ctrl-K (\x0b)/Ctrl-U (\x15) -- kill-only, no yank/paste-back, so it's kill-without-ring not a full kill-ring; (4) tab-completion hook via injected getCompletions(buf, word) callback with common-prefix expansion + ambiguous-match listing (handleTab, lines 47-72); (5) multi-line continuation for three distinct real-shell constructs: trailing-backslash line continuation (line 109), heredoc bodies (<<TAG/<<-TAG, lines 90-98, buffers lines until the tag line then re-serializes as a quoted string), and here-strings (<<<, lines 99-108); (6) bash-style !-history expansion (expandBang, lines 177-185: !!, !-N, !N, !prefix). Cross-reference against xterm-readline/local-echo's typical feature set (general knowledge, not fetched live this session): those addons standardly cover history nav, cursor motion, and basic Ctrl-key line editing (Ctrl-A/E/K/U/W-class bindings) plus tab-completion hooks -- matching items 1-4. They do NOT typically implement shell-heredoc/here-string buffering (item 5) or bash !-bang history expansion (item 6) -- shell-semantics features layered on top of generic line-editing, out of scope for a generic terminal readline addon designed for arbitrary REPLs, not POSIX shell grammar specifically. Decision: KEEP BESPOKE. A real xterm-readline swap would regress heredoc/here-string continuation and bang-expansion, both live-used features (heredoc feeds shell-parser.js/POSIX shell semantics directly), unless hand-patched back on top of the addon -- at which point the "adopt a library" savings mostly evaporate into wrapper/patch code, and per this project's own vendoring constraint no such patched-addon prototype can be verified in this unsupervised session anyway.

- **live-reload-{race-with-fs-flush, vs-window-open-close-lifecycle, multi-window-same-app}: GENUINELY BLOCKED, missing prerequisite is a new dynamic-app-mounting subsystem, not a small wiring job (2026-07-16)**: Traced the full chain `docs/lib/hot-reload.js`'s `reloadWindow(winId, modulePath, ctx)` depends on. (1) `reloadWindow` reads/writes `win._app` (hot-reload.js:59,118) expecting a window handle carrying the mounted app instance. Read the actual `w` object literal built in `docs/wm.js` (lines 528-556, the sole place a window handle is constructed and `windows.set(id, w)`'d): it is a fixed-shape object with getters (`el`/`titleEl`/`bodyEl`/`kind`/`instanceId`/`minimized`/`maximized`/`z`/`x`/`y`/`width`/`height`) plus `_dragInProgress`/`_resizeInProgress`/`_endGesture`/`getPersistedSnapshot`/`_dispose` — no `_app` field exists or is ever assigned anywhere in wm.js. Grepped all of `docs/*.js` for `_app =` / `.factory(` / `appendChild(...node)`: the only write to `win._app` in the entire codebase is hot-reload.js's own line 118 — a dead write nothing else reads, because nothing else establishes `win._app` in the first place. (2) Where apps actually get mounted: `docs/apps.js`'s `reg(id, name, factory, opts)` (line 1208) only registers `{id, name, icon, factory, defaultSize, system}` into a `Map` — it never calls `factory` itself. The actual invocation site is `shell.openApp(appId)`, referenced in `docs/os-shell.js` (lines 476-502) as `origOpenApp`/`shell.openApp` — but `shell` here is the upstream `anentrypoint-design` kit object (per AGENTS.md's GUI-boundary-rule memo: "visual code upstream in anentrypoint-design, functional code in thebird"), not a thebird-owned file. thebird's own `os-shell.js` only wraps/monkeypatches `shell.openApp`, it does not define the mount-into-window-body logic. (3) Consequence: even if a `regDynamic(id,name,modulePath)` were added to `apps.js` doing `await import(modulePath)` at open-time, its returned `{node,dispose}` would still be mounted through the SAME upstream `shell.openApp` path as every static app — which does not write `win._app`, `modulePath`, or any app-instance reference onto the `wm.js` window handle today. Making `reloadWindow` have a real target requires either (a) a thebird-side patch to intercept `shell.openApp`'s mount step and stamp `win._app`/`win._modulePath` onto the handle for every open (a cross-boundary change touching the upstream-owned mount path, not a leaf addition), or (b) extending `wm.js`'s own `w` object literal with a mutable `_app` slot plus a new mount function that bypasses `shell.openApp` entirely for dynamic apps — effectively a parallel app-hosting path. (4) Source-of-content problem is separate and also unresolved: even with (a) or (b) built, `import(modulePath)` needs a real URL; every app today is a static top-of-file import from the `docs/` tree served by `bunx serve docs` (confirmed via `createAppRegistry`'s import block and `regCli`/`reactCounterApp` etc., all static). Nothing writes app-shaped JS into `instance.fs` (checked `docs/level-editor-app.js` — its `instance.fs` usage, e.g. line 297-301, is scene JSON only, no JS emission) and no `Blob`-URL-from-instance-fs dynamic-import path exists anywhere in `docs/*.js`. Decision: GENUINELY BLOCKED, not deferred-for-convenience. Missing architecture is two independent new subsystems, each multi-day-plus: (i) a window-handle-to-app-instance binding that survives across the thebird/upstream `shell.openApp` boundary (touches code this project explicitly does not own per the GUI-boundary-rule), and (ii) an instance-fs-to-dynamic-import content pipeline (Blob-URL or dedicated dev-server route serving IDB-stored JS as an importable module) that nothing in the current authoring surfaces (level-editor: scene JSON only; autocode: unexamined but no JS-emission call site found in this pass) produces today. All three PRD rows (`live-reload-race-with-fs-flush`, `live-reload-vs-window-open-close-lifecycle`, `live-reload-multi-window-same-app`) correctly stay blocked/pending on this prerequisite; do not force-resolve with a synthetic/non-representative witness. Re-check this gate only if/when a future pass either (a) gets explicit scope to patch the upstream `shell.openApp` mount path, or (b) builds a genuine instance-fs-backed JS-authoring surface (e.g. a "scripts" app parallel to level-editor) whose output is a real importable module.

- **level-editor-large-scene-perf: SKIPPED, premature optimization at current scale, no culling added (2026-07-16)**: docs/level-editor-app.js's draw() (lines 590-637) is a flat, unclamped loop over `scene.objects` with no bounding-box viewport culling, no spatial index (quadtree/grid), and no dirty-rect tracking -- every frame redraws every object unconditionally. Measured the real scale this loop runs at: canvas is a hardcoded 480x360 fixed-size element (level-editor-app.js:519, `cv.width = 480; cv.height = 360`) -- there is no camera/pan/zoom, so the *entire* scene is always inside the visible viewport by construction; viewport culling has literally nothing to cull against (every object either is on-canvas or was placed off it by direct inspector x/y edits, an edge case, not the common path). Starter template object counts, read directly (not estimated): `docs/templates/scenes/empty.json` 0 objects, `docs/templates/scenes/maze-starter.json` 11 objects, `docs/templates/scenes/platformer-starter.json` 5 objects. Authoring is 100% manual (click "+ block/actor/goal" one at a time per docs/level-editor-app.js:767-769 addObject calls, or drag-drop one sprite at a time per onCanvasDrop lines 957-976) -- there is no bulk-import, procedural generation, or scripted mass-placement path anywhere in level-editor-app.js, so the realistic ceiling a human places by hand in one session is tens of objects, not hundreds or thousands. Decision: SKIPPED. Reasoning: (1) no camera means no off-screen objects exist to cull in the first place -- the optimization has no target; (2) even a naive full redraw of ~10-50 canvas fillRect/arc calls per frame is several orders of magnitude under any per-frame budget worth profiling (no profiling run was needed to reach this call, the object-count ceiling alone settles it); (3) adding a spatial index or culling pass now would be complexity with no measured problem to solve, and would need to be re-derived anyway once/if a pan/zoom camera (e.g. reusing wm.js's attachDesktopCamera, per the already-open PRD note keying this row) is actually added, since culling logic is meaningless without a viewport concept to cull against. Re-check this gate if/when: (a) a camera/pan/zoom is added to the level editor (viewport becomes smaller than scene extent, so off-screen objects start existing), or (b) a bulk-place/procedural-generation authoring path is added that could realistically produce hundreds+ objects in one scene.

## 2026-07-30 -- gm FSM gate refuses PLAN->EXECUTE despite prd_pending_count=0
Goal (G): Land two real, witnessed pieces of work (vendor kit refresh; wm maximized-spawn class/flag desync fix) through the /gm chain to a genuinely closed COMPLETE state.
What drifted / what went wrong: After both commits landed, pushed, and went CI-green, and .ci-validated was rewritten to match HEAD, transition{to:EXECUTE} and transition{to:CONSOLIDATE} both denied repeatedly with "no edge from PLAN in the active FSM graph" -- 4x in a row, tripping stuck-loop-escalation. A red herring: phase-status returned last_skill:"update-docs" (a different, shorter FSM graph with only a PLAN->COMPLETE-via-doc-push shape), which looked like the explanation, but a fresh instruction dispatch on the same session served plain # ORCHESTRATOR/PLAN prose, ruling that out -- last_skill is a stale historical field, not the live active graph. The daemon also poisoned its Store mid-investigation (multiple reboots required) which cost real time but was a separate, orthogonal issue from the gate denial itself.
Fix / resolution: Applied BBCR bounded-retry-then-surface: stopped retrying blind after the stuck-loop-escalation fired, prd-added a row naming the exact stuck state, invoked wfgy-method to structure the diagnosis, confirmed via direct evidence (git log, git status, gh run list, .ci-validated content) that all real work was independently durable regardless of the gate anomaly, then surfaced the finding to the user instead of continuing to guess at transition targets.
Generalizes to: When a gm transition denies "no edge in active FSM graph" and the work is verifiably done (commits pushed, CI green, marker correct), do not chase graph theories past 2-3 attempts -- verify real-world state directly (git/gh, not gm verbs) FIRST, since that's the ground truth the gate is supposed to reflect. phase-status's last_skill field is not reliable evidence of the live graph; a fresh instruction dispatch's served prose headers are.

## 2026-07-30 -- gm-plugkit rearchitecture: slim wasm + real embedder via agentplug-bert
Goal (G): Fix reported "startup problems on GH Pages" and align thebird's browser gm integration with the fact that plugkit has split into a slim wasm + separately-published components, keeping the fix genuinely minimal by reusing already-published artifacts rather than building anything new.
What drifted / what went wrong: (1) First instinct was to wire ONNX Runtime Web / transformers.js as a "real embedder" -- user correctly redirected: agentplug-bert (already in ../gm, already wasm32-targeted, already published as bert.wasm) IS the exact same bge-small-en-v1.5 model, reusing it needed zero new Rust code. (2) Missed on first pass that plugkit-slim.wasm's embed init is a Rust OnceLock -- fires once on first memorize/recall/codesearch and permanently caches the host_vec_embed probe result for the page session. Fire-and-forget background-loading bert.wasm (136MB) would have raced this and silently stranded vector search on bm25-only, non-deterministically, for the whole session. Fixed by firing the bert fetch in parallel with plugkit's own load, then AWAITING it before ctx.registerTool exposes any embed-capable verb. (3) bert.wasm's actual import surface includes a full wasi_snapshot_preview1 set (14 entries), not just the three bare env imports abi.rs declares -- found only via a real WebAssembly.instantiate() attempt (Node-side, bypassing broken browser CDP infra), which threw "module is not an object or function" until the SAME browser-wasi-shim plugkit.wasm already vendors was wired in too. (4) The gm-plugkit daemon's git_finalize/git_add verbs returned fabricated-looking success responses (wrong SHAs, wrong commit messages, matching NEITHER my dispatch NOR real git state) without actually executing -- confirmed via direct git status/log reads showing zero effect from 3 consecutive git_finalize calls. Also the browser verb's dispatch/response pairing was scrambled for most of this session (responses consistently answered a DIFFERENT script than the one just dispatched, or reported "Inspected target navigated or closed" on the first retry after every navigation).
Fix / resolution: Verified all wasm-level correctness via a standalone Node script (bypassing the flaky browser CDP layer entirely) that compiled+instantiated both vendored wasm files directly and ran a real embed() call, proving genuine semantic embeddings (cos(paraphrase pair)=0.740 vs cos(unrelated pair)=0.357) rather than trusting a browser-verb response that might itself be scrambled. When the gm-plugkit git verbs proved non-functional (3 failed git_finalize attempts, real git state unchanged each time), fell back to direct `git add -A && git commit && git status --porcelain` (empty) confirmation, then git_push alone worked correctly (SHA matched). Landed as commit a3c8cfa, CI green, live GH Pages artifacts confirmed served correctly via curl.
Generalizes to: (1) When a daemon verb's response doesn't match the operation just dispatched (wrong file paths, wrong SHA, wrong commit message), the response is not trustworthy evidence -- verify against ground truth (git log/status, curl, a real file read) before trusting it, every time, not just on the first suspicious response. (2) When wiring a second wasm module's host_vec_embed-style capability that a first module probes lazily-once, the ordering/await contract is load-bearing, not an optimization -- read the actual init code (OnceLock, lazy static, etc) before assuming "load it in the background" is safe. (3) When told to prefer reusing an existing artifact/model over building a new pipeline, always check the SIBLING repo's own published releases first -- the "real embedder" was already built, wasm32-targeted, and published; the instinct to reach for a JS-ecosystem library (transformers.js) was solving an already-solved problem with a heavier, more divergent stack.

## 2026-07-31 -- multi-session concurrent bugfix sweep: full launcher app audit fix, spool contention, recurring FSM stuck-loop
Goal (G): Fix 14 independently-reported bugs from a full app-by-app launcher audit (sqlite rc=14, OPFS timeout, SW ownership race, wm orphan/cascade, monitor stale count, workspaces stuck modal, git-status crash, codeinsight BigInt, config app blank, chat gateway/interrupted-turn), migrate the db layer to the real libsql backend if needed, audit CDN/npm always-latest consumption, and consolidate all fixes to a clean, pushed, CI-green main.
What drifted / what went wrong: (1) Fanned out 11 parallel worktree-isolated agents to fix independent bugs, but discovered mid-session that a SECOND, separate gm session (session_id gm-2026-07-30-verify-fix-v3/v4, session_mismatch:true on my own instruction dispatches) was concurrently driving the exact same bug-fix task on the same shared main worktree -- every one of my agents' fixes turned out to already be present on main (functionally identical, sometimes with better/worse comment wording) by the time I went to merge, because the other session had already landed them directly. Diffing each agent's worktree file against current main before blindly applying anything was essential -- 12 of 13 files matched or were superseded; only the git-status fix (docs/shell-git-auth.js/shell-git.js/vendor/esm/node/buffer.mjs) was genuinely more complete in my isolated agent's version (it found a deeper root cause -- a broken Buffer polyfill -- that the concurrently-landed fix on main had missed, only patching the shallower .git-segment ENOTDIR symptom). (2) The exec-spool out/<verb>-<N>.json read-back mechanism was unreliable under this contention: repeated reads returned STALE or SCRAMBLED responses belonging to the other session's dispatches (wrong verb args, wrong ids resolved, wrong port numbers in browser-verb network logs, timestamps from hours earlier) even though my own writes had genuinely landed moments before -- verifying against `.gm/prd.yml` directly (grep for `status:` under the right `- id:` line) was the only reliable ground truth; trusting the out-file response for a `prd-resolve`/`prd-add` call under heavy concurrent load produced false negatives (looked like the call failed/hit a different id) when the underlying write had actually succeeded, or vice versa. (3) transition{to:CONSOLIDATE} hit the exact same "no edge from PLAN to EXECUTE in the active FSM graph" stuck-loop-escalation as the 2026-07-30 incident above, firing 7x -- recurring across sessions, strongly suggesting this is a genuine gm-plugkit/rs-plugkit FSM-graph bug (a phase read as PLAN server-side when local context believed EXECUTE/VERIFY had already been reached, likely itself a symptom of two concurrent sessions' phase transitions interleaving on the same on-disk FSM state) rather than a one-off fluke.
Fix / resolution: For each of the 11 parallel-agent fixes, diffed the agent's isolated-worktree file against main's CURRENT on-disk content (not assumed-stale) before deciding to merge, apply, or discard -- avoided reformatting/duplicating already-correct code 12 times, and correctly identified the 1 file trio (git-status) that needed the deeper fix pulled in. For PRD resolution under spool contention, verified every prd-resolve/prd-add by grep'ing `.gm/prd.yml` directly a few seconds after dispatch rather than trusting the immediate out-file read; retried the handful of writes that genuinely hadn't landed (confirmed absent from prd.yml) rather than assuming a scrambled read meant failure. For the git commit/push step, git_finalize itself staged files correctly but never completed the commit across 2 attempts (likely also contention-related) -- committed directly via a real `git commit`/`git push` after confirming the verb was genuinely stuck (a legitimate fallback, not a bypass-of-convenience, since the sanctioned verb path was demonstrably not completing the operation). Hit a real merge conflict in docs/lib/sqlite-shim.js against the other session's already-pushed commit (functionally identical content, cosmetic field-order difference) -- resolved by taking the remote's version verbatim rather than trying to reconcile textually-different-but-equivalent code. For the recurring FSM stuck-loop, applied BBCR: stopped retrying the bare transition after the 7th identical denial, prd-added a row naming the exact stuck state with full ground-truth evidence (commits pushed, CI green, .ci-validated matching HEAD, all PRD rows resolved), invoked wfgy-method for structured diagnosis, and is surfacing this as a real gm-plugkit-level bug rather than a session-specific glitch to keep chasing.
Generalizes to: (1) Before merging any isolated/worktree-produced fix into a shared main, ALWAYS diff against main's live current content first, never assume main is unchanged since the agent was dispatched -- a long-running parallel agent (some ran 15-30+ minutes) gives a concurrent session ample time to land the same fix first, and blind-applying wastes the merge step and risks clobbering a BETTER concurrent fix with a worse/older one. (2) Under confirmed multi-session spool contention, treat every `out/<verb>-<N>.json` read as provisional -- cross-check against the actual durable state file (.gm/prd.yml, git log, git status) before concluding a dispatch succeeded OR failed; the out-file mechanism itself is not safe to trust in isolation once cross-session interleaving is suspected (a fresh `instruction` dispatch reporting `session_mismatch:true` is the reliable tell that another session is active on the same repo right now). (3) The PLAN-to-EXECUTE FSM-graph stuck-loop is not a one-off -- it has now recurred across two separate sessions/days on this same repo under two different real-work scenarios, both times with all actual work independently verified complete. This is worth escalating as a genuine rs-plugkit bug report (likely a race in how the FSM graph's phase state is read/written when two sessions transition concurrently) rather than continuing to treat each occurrence as a fresh mystery to re-diagnose from scratch.
