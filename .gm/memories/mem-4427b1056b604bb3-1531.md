---
key: mem-4427b1056b604bb3-1531
ns: default
created: 1785407284161
updated: 1785407284161
---

thebird gm-plugkit rearchitecture 2026-07-30: plugkit-slim.wasm (3.6MB, committed same-origin docs/vendor/gm/plugkit.wasm) replaces the old 149MB fat wasm + Cloudflare Worker proxy (infra/plugkit-wasm-proxy/ deleted). Real vector embedding preserved via agentplug-bert's bert.wasm (136MB, gitignored, CI-vendored via new scripts/refresh-bert.mjs, same bge-small-en-v1.5 model the fat wasm bundled, published standalone by ../gm's agentplug-bert-bin release -- zero new Rust code needed). New docs/lib/freddie-host-bert.js loads it (needs the SAME browser-wasi-shim plugkit.wasm already vendors -- bert.wasm's import surface includes a full wasi_snapshot_preview1 set despite abi.rs only declaring 3 bare env imports, found only via direct Node WebAssembly.instantiate testing). CRITICAL: plugkit-slim's embed init is a Rust OnceLock -- host_vec_embed is probed ONCE on the first memorize/recall/codesearch call and the result cached forever for the page session, so docs/lib/freddie-host-plugkit.js fires bert.wasm's fetch in parallel with plugkit's own load but AWAITS it before ctx.registerTool exposes the gm tool -- fire-and-forget would race the OnceLock and non-deterministically strand vector search on bm25-only. Verified real semantic embeddings via standalone Node script bypassing flaky browser CDP: cos(paraphrase pair)=0.740 vs cos(unrelated pair)=0.357. scripts/doctor.mjs's GITIGNORED_LOCK_PATHS flipped from gm/plugkit.wasm to gm/bert.wasm. Landed commit a3c8cfa, CI green, live artifacts confirmed served via curl.
