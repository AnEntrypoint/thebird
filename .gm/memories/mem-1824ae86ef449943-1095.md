---
key: mem-1824ae86ef449943-1095
ns: default
created: 1786108660701
updated: 1786108660701
---

thebird OPFS-primary filesystem detail (drained from AGENTS.md 2026-08-07): docs/instance-fs.js + docs/instance-fs-opfs.js (2026-07-17) -- real per-file OPFS is the PRIMARY backing store for the per-instance snapshot object. snapshot stays the same live in-memory object every consumer already reads/writes synchronously -- only the durability layer changed. IndexedDB is kept as a one-time migration source and the full fallback when OPFS is unavailable; fs.usingOpfs exposes which backend is live. Per-key fast-path writes run alongside the existing debounced full-resync save via makeSerializedSaver. OPFS's createSyncAccessHandle is Worker-only, so all real OPFS I/O runs in a dedicated Worker (instance-fs-opfs.js WORKER_SRC) -- do NOT put a raw backtick inside that WORKER_SRC template literal's own JS comments, it prematurely closes the outer literal and silently crashes every instance's boot. docs/shell-node-opfs.js (a NO-OP dead overlay) was removed; fs.promises.* now correctly delegates to the sync fs functions. Verified live via scripts/witness-opfs-fs.mjs (8/8 assertions pass).
