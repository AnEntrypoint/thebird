---
key: mem-2f29499d738de331-704
ns: default
created: 1785632338272
updated: 1785632338272
---

thebird mermaid-in-Node channel (witnessed 2026-08-02): mermaid@11.16.0 is ESM-only (type:module, no main field) so require() fails 'Cannot find module' even with a correct path — dynamic import of dist/mermaid.core.mjs via pathToFileURL is the working path. jsdom globals (window, document, navigator, HTMLElement, SVGElement, Element, Node, CustomEvent, requestAnimationFrame) must be set BEFORE the import. Install isolated under scripts/.scratch/mmd-val with --cache scripts/.scratch/npm-cache (the machine's global npm cache held a corrupt require-from-string tarball causing sha512 mismatch). Result: mermaid.parse('graph LR; A-->B') -> PARSE_OK diagramType=flowchart-v2 in pure Node, no browser.
