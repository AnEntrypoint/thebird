---
key: mem-fc880bdc3d75f72a-919
ns: default
created: 1786263108037
updated: 1786263108037
---

thebird docs/vendor.lock.json hashing line-ending normalization (fixed 2026-07-17): scripts/refresh-common.mjs's sha256OfFile normalizes CRLF->LF before hashing (binary files sniffed by leading-NUL check hashed unchanged). Root cause of a 3-commit red witness-ci streak: .gitattributes forces eol=lf in the git blob, but a Windows checkout with core.autocrlf=true still gets CRLF bytes on disk, so a vendor-lock hash computed on Windows never matched Linux CI even with zero real drift. git status --porcelain reports clean in this state (git's own diff normalizes) -- verify with git show HEAD:<path> vs on-disk byte compare, not git status, when vendor.lock parity is in doubt. doctor.mjs treats gm/bert.wasm's 'missing on disk' as expected-pass (deliberately gitignored, ~136MB over GitHub's cap, CI-fetched from a Release). gm/plugkit.wasm is now the slim variant (~3.6MB), committed directly, no longer gitignored.
