---
key: mem-fd4d4ec20a50fa38-1160
ns: default
created: 1786263075117
updated: 1786263075117
---

thebird plugkit.wasm host_cwd import + per-instance SW module caching (2026-08-09): plugkit.wasm's env imports can grow between wasm builds without any thebird-side signal beyond the freddie-host-plugkit.js envProxy stub's console.error (added same session) -- host_cwd appeared as a new unrecognized import, silently stubbed to 0n before the fix. Implemented in docs/lib/freddie-host-gm-bridge.js as `host_cwd: () => packResult('/')` since thebird's per-instance fs is root-anchored with no shell-style cwd concept. Separately: live-witnessing a JS fix via the gm browser verb against a running bunx serve docs instance can show STALE behavior even after confirming server-side freshness via curl+ETag and direct Node import of the edited module -- the per-instance Service Worker (docs/sw-iN/index.js, static per AGENTS.md's SW-isolation architecture) caches modules independent of navigation/session resets. A stuck stale-witness loop should navigator.serviceWorker.getRegistrations()+unregister() and caches.keys()+delete() via the browser verb (or just restart the serve process, which also clears the port) before concluding a fix genuinely doesn't work.
